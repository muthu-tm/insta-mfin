import 'package:flutter/material.dart';

Widget customIconButton(icon, size) {
  return IconButton(
    icon: Icon(
      icon,
      color: Colors.black,
      size: size,
    ),
    onPressed: () {},
  );
}
